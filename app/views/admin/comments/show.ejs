<div class="page-header">
    <h1>Comment Details</h1>
</div>

<table class="table table-bordered">
    <tbody>
        <tr><th>Comment Author</th><td><%- comment.userName %></td></tr>
        <tr><th>User System Name</th><td><%- user.displayName %></td></tr>
        <tr><th>Post Commented On</th><td><%- link_to(post.title, path_to.admin_post(post)) %></td></tr>
        <tr><th>Content of Comment</th><td><%- comment.content %></td></tr>    
    </tbody>
</table>

<div class="well">
    <%- link_to('<i class="icon-flag icon-white"></i> Flag', path_to.edit_admin_comment(comment), {class: 'btn btn-primary'}) %>
    <%- link_to('<i class="icon-remove icon-white"></i> Delete', path_to.admin_comment(comment), {class: 'btn btn-danger', method: 'delete', remote: true, jsonp: '(function (u) { location.href = u; })'}) %>
    or
    <%- link_to('Back to index', path_to.admin_comments()) %>
</div>
