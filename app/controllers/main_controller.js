load('application');

action('index', function () {
  render({
    title: "main#index"
  });
});

action('contact', function () {
  render({
    title: "main#contact"
  });
});

action('about', function () {
  render({
    title: "main#about"
  });
});